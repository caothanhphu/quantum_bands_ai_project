# File: .env.example

# Project Settings
PROJECT_NAME="Quantum Bands AI Investment Fund Management System"
PROJECT_VERSION="0.1.0-mvp"
LOG_LEVEL="INFO" # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# MSSQL Database Configuration
DB_SERVER=your_mssql_server_address_or_hostname
DB_PORT=1433 # Cổng mặc định của MSSQL
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_NAME=your_database_name
# Quan trọng: Đảm bảo ODBC driver này đã được cài đặt trên hệ thống của bạn
DB_DRIVER="{ODBC Driver 17 for SQL Server}" # Ví dụ: "{ODBC Driver 18 for SQL Server}", "SQL Server Native Client 11.0"

# Tùy chọn: Nếu muốn dùng connection string đầy đủ trực tiếp (ít khuyến khích hơn việc cấu hình từng phần)
# DATABASE_URL="mssql+pyodbc://<DB_USER>:<DB_PASSWORD>@<DB_SERVER>:<DB_PORT>/<DB_NAME>?driver=<DB_DRIVER_URL_ENCODED>"